# 换成你的域名，Caddy 会自动签证书
your-domain.com {
	forward_proxy {
		# 改成自己的账号密码，公网上不加认证等于开门揖盗
		basic_auth myuser myStrongPass
		hide_ip
		hide_via

		# 需要级联 tinyproxy 时取消注释
		# upstream tinyproxy:8888
	}

	# 顺便挂个页面，看起来更像普通网站
	respond "Hello" 200
}
